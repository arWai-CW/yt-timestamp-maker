{
	"name": "yt-timestamp-maker",
	"version": "1.0.0",
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "^25.5.2",
		"typescript": "^5.0.0",
		"vite": "^5.0.0"
	},
	"description": "Create, manage, and export timestamps with notes on YouTube videos",
	"private": true,
	"scripts": {
		"dev": "vite",
		"build": "BUILD=extension vite build && cp dist/content.js extension/ && cp dist/style.css extension/content.css",
		"preview": "vite preview"
	},
	"type": "module"
}
